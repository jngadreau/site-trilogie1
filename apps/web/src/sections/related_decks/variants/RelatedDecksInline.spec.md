# RelatedDecksInline

Liste **compacte** de jeux liés : lien + une ligne de description markdown par entrée.

## Props JSON

| Champ | Type | Notes |
|-------|------|--------|
| `sectionTitle` | string | H2 |
| `introMarkdown` | string | optionnel |
| `items` | array | ≥ 2 `{ "label", "descriptionMarkdown", "href" }` |

## Slots médias

**`media` : `[]`**
