# OutcomesSignalStrip

Section **`outcomes`** : liste **horizontale / bandeau** avec marqueurs type « signal » (pulse) et libellé + détail.

## Props JSON

| Champ | Type | Notes |
|-------|------|--------|
| `sectionTitle` | string | H2. |
| `introMarkdown` | string | Court. |
| `signals` | array | **≥ 3** objets `{ "label", "detailMarkdown" }`. |

## Slots médias

`[]` sauf besoin futur.
