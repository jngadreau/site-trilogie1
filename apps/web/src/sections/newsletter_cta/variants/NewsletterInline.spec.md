# NewsletterInline

Bloc centré : texte + champ e-mail + bouton (formulaire **démo** — branchement backend à prévoir).

## Props

- `sectionTitle`, `bodyMarkdown`, `fieldLabel?`, `buttonLabel`, `footnoteMarkdown?`

## Slots médias

`media` : `[]`.
