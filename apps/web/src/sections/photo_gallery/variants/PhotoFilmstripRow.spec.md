# PhotoFilmstripRow

**3 à 5** images en **ligne** (format paysage ou carré léger), légendes courtes optionnelles.

## Props

- `sectionTitle`, `introMarkdown?`
- `items` : `{ imageUrl, alt, label? }[]`

## Slots médias

`media` : `[]`.
