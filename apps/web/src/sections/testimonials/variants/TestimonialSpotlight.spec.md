# TestimonialSpotlight

**Une** citation mise en avant, photo optionnelle.

## Props

- `sectionTitle`, `introMarkdown?`, `quoteMarkdown`, `name`, `roleLine?`, `imageUrl?`, `imageAlt?`

## Slots médias

`media` : `[]` ou slot unique si génération portrait.
