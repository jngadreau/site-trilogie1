# HeroCardsStrip

Hero **bandeau** : rangée horizontale de cartes (défilement tactile sur petit écran), texte centré ou aligné au-dessus.

## Props JSON

| Champ | Type | Notes |
|-------|------|--------|
| `title` | string | H1 |
| `subtitle` | string | Sous-titre / accroche |
| `bodyMarkdown` | string | |
| `ctaLabel` | string | |
| `ctaHref` | string | |
| `cards` | array | **4 à 9** objets — `imageUrl` = **`/ai/generated-images/deck-cards/...`** (voir sync site) |

## Slots médias

**`media` : `[]`**
