# CtaMarqueeRibbon

Section **`cta_band`** : **ruban** avec ligne de texte **défilante** (mots-clés) + bloc central titre / sous-titre / CTA.

## Props JSON

| Champ | Type | Notes |
|-------|------|--------|
| `eyebrow` | string | Court. |
| `headline` | string | H2 fort. |
| `subline` | string | Une phrase. |
| `ctaLabel` | string | |
| `ctaHref` | string | |
| `marqueeText` | string | Suite de mots ou expressions séparées par ` · ` (seront répétées en boucle). |

## Slots médias

`[]`.
