# CtaSplitAction

Section **`cta_band`** : **deux colonnes** — texte + Markdown à gauche, **boutons** à droite (primaire obligatoire, secondaire optionnel).

## Props JSON

| Champ | Type | Notes |
|-------|------|--------|
| `title` | string | H2. |
| `bodyMarkdown` | string | 1–2 paragraphes. |
| `primaryLabel` | string | |
| `primaryHref` | string | |
| `secondaryLabel` | string optionnel | |
| `secondaryHref` | string optionnel | |

## Slots médias

`[]`.
