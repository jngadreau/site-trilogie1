/** Slot image optionnel : URL appliquée comme fond CSS de la section (`section.backgroundImage`). */
export const SECTION_BACKGROUND_SLOT_ID = 'section_background';

/** Scène par défaut (EN) pour Imagine / éditeur si aucune description stockée. */
export const SECTION_BACKGROUND_DEFAULT_SCENE_EN =
  'Full-bleed background plate behind section content, cohesive with deck visual identity, no text or UI.';
